package com.mongenscave.mcenchantgrind.utils;

import com.mongenscave.mcenchantgrind.McEnchantGrind;
import com.mongenscave.mcenchantgrind.commands.CommandEnchantGrind;
import com.mongenscave.mcenchantgrind.exceptions.CommandExceptionHandler;
import com.mongenscave.mcenchantgrind.identifiers.keys.ConfigKeys;
import lombok.experimental.UtilityClass;
import revxrsal.commands.bukkit.BukkitLamp;
import revxrsal.commands.orphan.Orphans;

@UtilityClass
public class RegisterUtils {
    //public void registerCommands() {
    //    var lamp = BukkitLamp.builder(McEnchantGrind.getInstance())
    //            .exceptionHandler(new CommandExceptionHandler())
    //            .build();
//
    //    lamp.register(Orphans.path(ConfigKeys.ALIASES.getList().toArray(String[]::new)).handler(new CommandEnchantGrind()));
    //}
}
